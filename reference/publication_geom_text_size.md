# Convert points to ggplot geom text size units

Convert points to ggplot geom text size units

## Usage

``` r
publication_geom_text_size(point_size)
```

## Arguments

- point_size:

  Font size in points.

## Value

Numeric size value for `geom_text(size = ...)`.
