% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/value_function_plot.R
\name{compare_value_function_types}
\alias{compare_value_function_types}
\title{Compare Different Value Function Types}
\usage{
compare_value_function_types(
  benefit_name = "Benefits",
  benefit_min = 0,
  benefit_max = 100,
  benefit_label = NULL,
  risk_name = "Risks",
  risk_min = 0,
  risk_max = 50,
  risk_label = NULL,
  n_points = 100,
  show_titles = TRUE,
  show_legend = TRUE,
  power = 2
)
}
\arguments{
\item{benefit_name}{Character string for the benefit criterion name.
Default is "Benefits".}

\item{benefit_min}{Numeric value for benefit minimum threshold.
Default is 0.}

\item{benefit_max}{Numeric value for benefit maximum threshold.
Default is 100.}

\item{benefit_label}{Character string for benefit x-axis label.
If NULL, uses benefit_name. Default is NULL.}

\item{risk_name}{Character string for the risk criterion name.
Default is "Risks".}

\item{risk_min}{Numeric value for risk minimum threshold.
Default is 0.}

\item{risk_max}{Numeric value for risk maximum threshold.
Default is 50.}

\item{risk_label}{Character string for risk x-axis label.
If NULL, uses risk_name. Default is NULL.}

\item{n_points}{Integer specifying number of points for plotting.
Default is 100.}

\item{show_titles}{Logical indicating whether to show plot titles.
Default is TRUE.}

\item{show_legend}{Logical indicating whether to show the legend.
Default is TRUE.}

\item{power}{Numeric value for the power/exponential function exponent.
Default is 2 (risk-averse, diminishing returns).}
}
\value{
A combined plot (using patchwork) showing value function type
comparisons for both benefits and risks side by side.
}
\description{
Creates a comparison plot showing multiple value function types
(Linear, Piecewise Linear, Exponential, Sigmoid, Step) overlaid on
the same plot. This visualization helps stakeholders understand how
different functional forms would transform the same raw clinical data,
and demonstrates why linear functions are the regulatory-preferred
default. Creates separate plots for benefits (increasing direction)
and risks (decreasing direction).
}
\examples{
# Default comparison
comparison_plot <- compare_value_function_types()

# Custom comparison with specific criteria
custom_comparison <- compare_value_function_types(
  benefit_name = "Efficacy",
  benefit_min = 0,
  benefit_max = 100,
  benefit_label = "Response Rate (\%)",
  risk_name = "Safety",
  risk_min = 0,
  risk_max = 50,
  risk_label = "Adverse Event Rate (\%)"
)

# Without titles for cleaner display
\dontrun{
comparison_clean <- compare_value_function_types(
  show_titles = FALSE
)
}
}
