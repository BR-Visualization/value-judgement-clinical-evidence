% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_to_event_scatter.R
\name{create_time_to_event_scatter}
\alias{create_time_to_event_scatter}
\title{Create Time-to-Event Scatter Plot: Benefit vs Risk Timing}
\usage{
create_time_to_event_scatter(
  data,
  vary_by = "benefit",
  time_units = "Days",
  add_marginals = TRUE,
  fig_colors = NULL
)
}
\arguments{
\item{data}{A data frame with subject-level time-to-event data containing:
\itemize{
\item subject_id: Unique subject identifier
\item time_to_benefit: Time to first benefit event (or censoring time)
\item benefit_type: Type of benefit outcome
\item time_to_risk: Time to first risk event (or censoring time)
\item risk_type: Type of risk outcome
\item benefit_observed: (optional) 1 = event observed, 0 = censored
\item risk_observed: (optional) 1 = event observed, 0 = censored
Censored observations are displayed at their censoring time with open circles and tick marks.
}}

\item{vary_by}{Character string specifying which dimension to vary by color/shape.
Options: "benefit" (vary by benefit type) or "risk" (vary by risk type).
Default is "benefit".}

\item{time_units}{Character string for time unit labels (e.g., "Days", "Weeks", "Months").
Default is "Days".}

\item{add_marginals}{Logical. If TRUE, adds marginal density plots. Default is TRUE.}

\item{fig_colors}{Optional vector of colors for different event types.
If NULL, uses default color palette.}
}
\value{
A ggplot object showing the time-to-event scatter plot with unity line.
Points below the unity line indicate risk occurred before benefit (unfavorable).
Points above the unity line indicate benefit occurred before risk (favorable).
}
\description{
Create Time-to-Event Scatter Plot: Benefit vs Risk Timing
}
\details{
This visualization helps identify temporal patterns in benefit-risk profiles:
\itemize{
\item Unity line (y=x): represents equal timing of benefit and risk
\item Below line: risk precedes benefit (less favorable patient experience)
\item Above line: benefit precedes risk (more favorable patient experience)
}

The plot can highlight whether certain benefit or risk types tend to occur
earlier in treatment, which may inform:
\itemize{
\item Treatment adherence strategies
\item Patient counseling and expectation setting
\item Risk mitigation timing
}

Censored observations are shown with:
\itemize{
\item Open circles (hollow shapes) instead of filled
\item Tick marks (+) to indicate censoring direction
\item Time displayed is the censoring time (e.g., end of follow-up)
}

Note: Summary statistics (mean, benefit-before-risk \%) are calculated only
among subjects with both events observed. Consider competing risks and
informative censoring in interpretation.
}
\examples{
\dontrun{
# Create example data
time_data <- data.frame(
  subject_id = 1:100,
  time_to_benefit = rexp(100, rate = 0.05),
  benefit_type = sample(c("Symptom Relief", "Clinical Response"), 100, replace = TRUE),
  time_to_risk = rexp(100, rate = 0.03),
  risk_type = sample(c("Mild AE", "Moderate AE"), 100, replace = TRUE)
)

# Create plot varying by benefit type
create_time_to_event_scatter(
  data = time_data,
  vary_by = "benefit",
  time_units = "Days"
)

# Create plot varying by risk type
create_time_to_event_scatter(
  data = time_data,
  vary_by = "risk",
  time_units = "Weeks"
)
}
}
